/** Router alternativo (BrowserRouter). La app usa el de main.tsx. */
import { BrowserRouter } from 'react-router'

export const Router = () => {
  return (
    <BrowserRouter>

    </BrowserRouter>
  )
}
